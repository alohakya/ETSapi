<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.project.etsapi.mapper.TeachCourseMapper">

    <!--add-->
    <insert id="addTeachCourse" parameterType="com.project.etsapi.entity.TeachCourse">
        INSERT INTO teachcourse (teacher_ID, course_ID)
        VALUES (#{teacher_ID}, #{course_ID})
    </insert>

<!--    get Teacher List By Course_ID-->
    <select id="getTeacherListByCourseId" parameterType="String" resultType="com.project.etsapi.entity.Teacher">
        SELECT teachcourse.teacher_ID, teacher.name
        FROM teachcourse NATURAL JOIN teacher
        WHERE teachcourse.course_ID = #{course_ID}
    </select>

<!--    get TeachCourse-->
    <select id="getTeachCourse" parameterType="java.util.HashMap" resultType="com.project.etsapi.entity.TeachCourse">
        SELECT * FROM teachcourse
        WHERE teacher_ID=#{teacher_ID} and course_ID=#{course_ID}
    </select>
</mapper>



